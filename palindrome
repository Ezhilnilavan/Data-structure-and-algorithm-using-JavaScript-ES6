palindrome(str = "racecar"){
        let flag = true;
        [...str].forEach((v,i,arr)=>{
            ++i;
            if(v !== arr[arr.length - i]){
                flag = false;
            }
        });
        return flag;

}
